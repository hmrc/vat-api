{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "id": "payment-indicator",
  "description": "DD - if customer is on Direct Debit BANK - customer has provided bank details.",
  "oneOf": [
    { "enum": ["DD"], "description": "Direct Debit"},
    { "enum": ["BANK"], "description": "Bank details"}
  ],
  "example": "DD"
}